// Part of <https://miracle.systems/p/walkner-paltrack> licensed under <CC BY-NC-SA 4.0>

define([

], function(

) {
  'use strict';

  return function decoratePalletKind(model)
  {
    var obj = model.toJSON();

    return obj;
  };
});
